/** Daily user cap for the hosted (no-BYOK) tier. */
export const SOCIAL_POST_DAILY_USER_CAP = 5;

/** Daily overall generation pool for the hosted (no-BYOK) tier. */
export const SOCIAL_POST_DAILY_SHARED_POOL = 1000;
